"""Training and calibration helpers for the FastAPI sidecar.

This package intentionally stays lightweight: we start with simple calibration models
trained from /feedback logs and loaded by the sidecar at runtime.
"""
